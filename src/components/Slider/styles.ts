import { styled } from '../../styles';
import { Swiper } from 'swiper/react';


export const SliderContainer = styled(Swiper, {
  '.swiper-button-prev': {
    color: '$white'
  },
  '.swiper-button-next': {
    color: '$white',
  },
})
